#[allow(dead_code)]
#[derive(Debug)]
pub enum Direction {
    TopLeft,
    Top,
    TopRight,
    
    RightTop,
    Right,
    RightBottom,

    BottomRight,
    Bottom,
    BottomLeft,

    LeftBottom,
    Left,
    LeftTop
}